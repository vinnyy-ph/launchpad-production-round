/**
 * Field names used in document-review validation error responses.
 */
export const DOCUMENT_REVIEW_FIELDS = {
  SUBMISSION_ID: "submissionId",
  STATUS: "status",
  REJECTION_NOTE: "rejectionNote",
} as const;
