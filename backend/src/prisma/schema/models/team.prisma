model Team {
  id        String   @id @default(uuid())
  name      String
  leaderId  String
  leader    Employee @relation("TeamLeader", fields: [leaderId], references: [id])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  members              TeamMember[]
  surveyAudienceConfig SurveyAudienceConfig[]

  // ── Audit ──
  createdBy String?
  updatedBy String?
  deletedAt DateTime?
  deletedBy String?

  @@map("teams")
}
