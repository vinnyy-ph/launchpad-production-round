model ClearanceSignatureRequest {
  id            String            @id @default(uuid())
  offboardingId String
  offboarding   OffboardingRecord @relation(fields: [offboardingId], references: [id])
  signatoryId   String
  signatory     Employee          @relation(fields: [signatoryId], references: [id])
  /// Snapshot copied from the template at offboarding initiation — later edits
  /// to the template must NOT mutate an in-flight clearance.
  purpose       String
  requirements  String
  status        SignatoryStatus   @default(PENDING)
  note          String? // optional on sign, required on reject
  actionAt      DateTime?

  // ── Audit ──
  createdAt DateTime  @default(now())
  createdBy String?
  updatedAt DateTime  @updatedAt
  updatedBy String?
  deletedAt DateTime?
  deletedBy String?

  @@unique([offboardingId, signatoryId])
  @@map("clearance_signature_requests")
}
