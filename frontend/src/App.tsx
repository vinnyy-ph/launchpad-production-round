export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-8 text-center">
      <h1 className="text-3xl font-semibold tracking-tight">launchpad-production-erp</h1>
      <p className="text-muted-foreground">
        Base skeleton ready. Add shadcn components with{" "}
        <code className="rounded bg-muted px-1.5 py-0.5 text-sm">npx shadcn@latest add button</code>,
        then build out the module routes.
      </p>
    </main>
  );
}
